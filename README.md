# GridWise Hackathon Service

Energy optimization API service with natural-language constraint interpretation and deterministic guardrails.

## Setup & Local Run

1. **Environment Setup**
   ```bash
   cp .env.example .env
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   pip install -r requirements.txt